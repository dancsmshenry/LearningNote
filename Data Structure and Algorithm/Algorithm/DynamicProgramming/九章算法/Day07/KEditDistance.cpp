#include <iostream>

using namespace std;

int main(){
    /**
     * 描述
     * 给出一个只含有小写字母的字符串的集合以及一个目标串，输出所有可以经过不多于 k 次操作得到目标字符串的字符串。
     * 你可以对字符串进行一下的3种操作:加入1个字母,删除1个字母,替换1个字母
     * 状态转移方程：当前的状态=
     * 1、在前一个字符串上插入一个新的字符
     * 2、把前一个字符串的最后一个字符替换为新的字符
     * 3、删掉最后一个字符
     * 4、和前i-1的情况一样
     * 注意：这里有很多个字符串，即是一个字符串组，所以用字典树来减少时间消耗
     * 因为字典树感觉面试当中不怎么会考，所以这道题的代码就暂时不敲了
    **/ 
}